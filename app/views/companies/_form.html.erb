<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_company %><br>
    <%= f.text_field :name_company %>
  </div>
  <div class="field">
    <%= f.label :personal_id %><br>
    <%= f.number_field :personal_id %>
  </div>
  <div class="field">
    <%= f.label :code_company %><br>
    <%= f.text_field :code_company %>
  </div>
  <div class="field">
    <%= f.label :slug %><br>
    <%= f.text_field :slug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
