<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="assets/favicon.png">

    <title>Todo</title>
    <%= stylesheet_link_tag    "css/bootstrap", media: "all", "data-turbolinks-track" => true %>
    <%= stylesheet_link_tag 'css/login', "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>

    <!-- Custom styles for this template -->

  </head>

  <body>
    <div class="container">
    	<% if alert %>
	    	<div class="alert alert-warning fade in">
		      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
		      <strong><%= alert %></strong>
		    </div>
	    <% end %>
    	<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), html: { class: 'form-signin' }) do |f| %>
		    <h2 class="form-signin-heading">Sign in to <strong>User Application</strong></h2>
		    <%= f.email_field :email, :autofocus => true, class: 'form-control', placeholder: 'Email address', :required => true %>
		    <%= f.password_field :password, class: "form-control", placeholder: "Password", required: true %>
		    <%= f.submit "Sign in", class: "btn btn-lg btn-success btn-block" %>
			<!-- <%= link_to "Forgot your password?", new_password_path(resource_name) %> -->
		<% end %>
    </div> <!-- /container -->


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
  </body>
</html>
